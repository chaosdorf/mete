= semantic_form_for @user, :html => { :class => 'form-horizontal' } do |f|
  = f.semantic_errors
  = f.inputs do
    = f.input :name, hint: 'your nickname'
    = f.input :email, hint: 'your eMail address, visit gravatar.com to set you avatar'
    = f.input :balance, :as => :number, hint: 'just in case you need to correct this'
    = f.input :active
  = f.actions do
    = f.action :submit
