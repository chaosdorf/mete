.col-xs-6.col-sm-3.col-md-2.col-lg-2
  = link_to user, :title => user.name do
    .panel.panel-default.text-center
      .panel-header
        %h6= user.name
      .panel-body
        = gravatar(user)
